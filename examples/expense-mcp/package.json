{
  "name": "expense-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "dev": "concurrently \"npx @modelcontextprotocol/inspector\" \"wrangler dev\" --kill-others",
    "cf-typegen": "wrangler types --include-runtime=false",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "typescript": "catalog:",
    "@cloudflare/vitest-pool-workers": "catalog:",
    "@null-shot/test-utils": "workspace:*",
    "vitest": "catalog:",
    "concurrently": "^9.1.2",
    "wrangler": "catalog:"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "catalog:",
    "@null-shot/mcp": "workspace:*",
    "hono": "^4.0.0",
    "zod": "^3.23.8"
  }
}
